require('dotenv').config(); const mongoose = require('mongoose'); const User = require('./models/User'); mongoose.connect('mongodb://127.0.0.1:27017/echobox').then(async () => { if (!await User.findOne({ username: 'admin' })) { await User.create({ username: 'admin', password: 'password', isAdmin: true }); console.log('Admin created.'); } else { console.log('Admin exists.'); } process.exit(); });
